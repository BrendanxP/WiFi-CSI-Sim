function [AOA_profile, AOA_angle] = bartlett_wrap(ppData, betaList, gammaList, lambda)
    if ~exist("plotVisible","var")
        plotVisible = 'on'; % default to show
    end


    
    simple=false;
    
    
end

%[appendix]{"version":"1.0"}
%---
